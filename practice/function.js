// creating basic calculater by using funtions

'use strict';

let result = (a, b , op) => {
    return result = ((op == 1) ? (a + b) : (a * b));
}

// console.log(result(2, 4, 1));
console.log(result(2, 4, 2));