// Srting are also object in JS

var x = "vikash";

var phrase = "Don\'t";

Object = (Properties + Methods)

// Properties
phrase.length;

// Methods
phrase.toUpperCase();
		  .split(" ");                  // Creates a array
		  .indexOf("find the string");  // Find the string in String (if find the word true else gives -1)
  		.slice(6,5);

// Lower char are greater then Upper case alphabet
